version: '3.8'

services:
  sentiment-api:
    build: .
    container_name: sentiment-classifier
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
